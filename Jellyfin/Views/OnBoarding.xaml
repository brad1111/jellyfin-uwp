<Page
    x:Class="Jellyfin.Views.OnBoarding"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Jellyfin.Views"
    xmlns:c="using:Jellyfin.Controls"
    xmlns:vm="using:Jellyfin.ViewModels"
    xmlns:models="using:Jellyfin.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Unloaded="Page_Unloaded"
    mc:Ignorable="d">

    <Page.DataContext>
        <vm:OnBoardingViewModel/>
    </Page.DataContext>

    <Grid Background="{StaticResource Color0}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <c:DeviceFamilyStateTrigger TargetDeviceFamily="Desktop" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RowHeader.Height" Value="60" />
                        <Setter Target="logo.HorizontalAlignment" Value="Left" />
                        <Setter Target="logo.Height" Value="28" />
                        <Setter Target="logo.Margin" Value="28 0 0 0" />
                        <Setter Target="txtTitle.FontSize" Value="22" />
                        <Setter Target="pnlMain.MaxWidth" Value="400" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid.RowDefinitions>
            <RowDefinition x:Name="RowHeader" Height="200" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid Background="{StaticResource Color10}" Grid.ColumnSpan="2">
            <Image
                Source="/Assets/OnBoardingLogo.png" 
                Stretch="Uniform" 
                HorizontalAlignment="Center" 
                Height="80" />
        </Grid>

        <Grid Grid.Row="1" Padding="0 40 0 0">
            <StackPanel 
                x:Name="pnlMain" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Top" 
                Width="640">
                <TextBlock
                    x:Name="txtTitle"
                    Text="Connect to Jellyfin Server" 
                    Foreground="White" FontSize="{StaticResource FontL}"
                    Margin="0 0 0 28" FontFamily="{StaticResource JellyfinFamilyFont}"
                    HorizontalAlignment="Center" />
                <TextBox 
                    x:Name="txtUrl"
                    Style="{StaticResource PrimaryTextBox}" 
                    Margin="0 8 0 8"
                    PlaceholderText="ex: 192.168.1.100:8096 or demo.jellyfin.org"
                    Text="{Binding UriString, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                    KeyUp="TxtUrl_KeyUp"/>
                <TextBlock 
                    x:Name="txtError"
                    FontFamily="{StaticResource JellyfinFamilyFont}"
                    Text="We're unable to connect to the selected server right now. Please ensure it is running and try again" 
                    HorizontalAlignment="Center"
                    TextAlignment="Center"
                    TextWrapping="Wrap"
                    Visibility="{Binding ConnectError}"
                    FontSize="{StaticResource FontM}"
                    Margin="1 1 1 20"
                    Foreground="#CF4A4A" />
                <Button 
                    x:Name="btnConnect"
                    Style="{StaticResource PrimaryButton}"
                    Content="Connect"
                    IsEnabled="{Binding ControlsEnabled}"
                    Click="BtnConnect_Click"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Grid.Column="1">
            <StackPanel 
    x:Name="pnlDiscoveryTitle" 
    HorizontalAlignment="Center" 
    VerticalAlignment="Top" 
    Width="640">
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
>
                    <TextBlock
        x:Name="txtDiscoverTitle"
        Text="Discover Server" 
        Foreground="White" FontSize="{StaticResource FontL}"
        Margin="0 0 0 28" FontFamily="{StaticResource JellyfinFamilyFont}"
                        Padding="0 40 0 0"
        HorizontalAlignment="Center" />
                </StackPanel>


                <ListView x:Name="DiscoveredList" IsItemClickEnabled="True" ItemClick="DiscoveredList_ItemClick" ItemsSource="{Binding DiscoveredServers}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:DiscoveredServer">
                            <StackPanel>
                                <TextBlock Text="{x:Bind Name}" FontSize="{StaticResource FontM}"/>
                                <TextBlock Text="{x:Bind Address}" FontSize="{StaticResource FontS}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.Footer>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock FontSize="{StaticResource FontL}" VerticalAlignment="Center">Searching</TextBlock>
                            <muxc:ProgressRing IsActive="True"/>
                        </StackPanel>
                    </ListView.Footer>
                </ListView>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
